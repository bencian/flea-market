<tr>
  <td><%= tag.name %></td>
  <td class='text-right'>
    <%= link_to t('button_options.edit'), edit_admin_tag_path(tag), class: 'btn btn-sm btn-outline-warning' %>
    <%= link_to t('button_options.delete'), admin_tag_path(tag), method: :delete, data: { confirm: 'Esta seguro?' }, class: 'btn btn-sm btn-outline-danger' %>
  </td>
</tr>